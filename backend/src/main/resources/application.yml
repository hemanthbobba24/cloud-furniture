spring:
  datasource:
    url: jdbc:mysql://localhost:3306/furniture
    username: root
    password: password
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  data:
    mongodb:
      uri: mongodb://root:password@localhost:27017/furniture?authSource=admin

server:
  port: 8080
