server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/furniture?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password: password
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    database-platform: org.hibernate.dialect.MySQL8Dialect
    hibernate:
      ddl-auto: update
    show-sql: true
  data:
    mongodb:
      uri: mongodb://root:password@localhost:27017/furniture?authSource=admin

# put custom app settings OUTSIDE of 'spring:' unless your @ConfigurationProperties expects otherwise
app:
  jwt:
    secret: "2f585bc1a2f3462bb4d3b3a3f2f9a9dd3f7a7f2c6f4e9a4c3b8f2e7a6d9c1b2a"
    expirySeconds: 86400

